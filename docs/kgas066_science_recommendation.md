# KGAS066 science recommendation (interim)

Based on the **2026-05-26** `diagnose_5kms_frozen` run (`uvkin b69bb65`) and the science-matrix infrastructure. Revisit after pilot matrix jobs complete.

## Scoreboard (baseline only; matrix pilots pending)

Generated by `scripts/aggregate_science_matrix.py` (baseline = existing `results/KILOGAS066`):

| ID | rchi2_MAP | mom0_corr (grid) | γ wall_hi | r_scale wall_lo | rank_score |
|----|-----------|------------------|-----------|-----------------|------------|
| baseline (5kms_A_vis equivalent) | 1.111 | 0.21 | 0.999 | 0.993 | -13.1 |

Pilot matrix jobs (`science_matrix/KILOGAS066/`) are defined but not yet run on ARC.

## Baseline findings (current production diagnostic)

| Metric | Value | Interpretation |
|--------|-------|----------------|
| Preflight inClouds mom0 corr | **0.98** | Frozen geometry valid on DR1 30 km/s |
| Vis flux audit seed | **36** Jy·km/s | mom0/data ≈ 2.6 → auto vis-aligned |
| MAP flux | **27** Jy·km/s | MAP/imaging mom0 **0.29** |
| Imaging-grid mom0 corr (gNFW MAP) | **0.21** | Poor; compact model + low flux |
| γ wall fraction (hi) | **~1.0** | Pinned at upper bound 2 |
| r_scale wall fraction (lo) | **~0.99** | Pinned at beam floor 1.31″ |
| τ_max(r_scale) | **154** | Needs long chain or is non-identifiable |
| Line excess power | **1.05** | Weak line vs off-line — caution for 5 km/s |

## Recommended profiles (until matrix completes)

### Production (visibility science)

**Profile:** `diagnose_30kms_frozen` + `--flux-seed-source auto`  
**Rationale:** Native channel width matches DR1 imaging (30 km/s); weak line SNR argues against relying on 5 km/s substructure. Frozen geometry retains validated PA/inc/vsys/centroid.

```bash
bash scripts/submit_kgas.sh KILOGAS066 diagnose_30kms_frozen
```

Requires `diagnose_30kms_frozen` in `submit_kgas.sh` (added with this matrix).

**First MCMC extension after matrix pilot:** `30kms_B_fixgamma_vis` if it reduces γ wall pressure without large `rchi2` penalty; else `30kms_C_fixrscale_vis` if r_scale wall is the dominant pathology.

### Validation (imaging morphology)

**Profile:** `diagnose_5kms_frozen` + `--flux-seed-source mom0` (experiment `5kms_A_mom0`)  
**Rationale:** Tests whether imaging-flux gNFW MAP can match DR1 cubes when visibility likelihood prefers lower flux. Use for **cube/mom0 QA only**, not as primary vis fit unless scoreboard shows competitive `rchi2`.

**Keep as reference:** existing `5kms_A_vis` baseline (`results/KILOGAS066/`) for regression on pipeline fixes.

## Explicit criteria to update this memo

After `aggregate_science_matrix.py` on pilot + long runs:

1. **Production** = lowest `rchi2_MAP` among runs with `imaging_grid_mom0_corr > 0.5` OR best compromise if none reach 0.5.
2. **Validation** = highest `imaging_grid_mom0_corr` with documented `MAP/imaging` flux ratio.
3. **5 vs 30 km/s** = adopt 5 km/s only if `5kms_*` posteriors are tighter (lower τ, lower walls) *and* line excess ≥ 1.5 on pre-fit diagnostics.

## Next actions

1. `bash scripts/submit_kgas066_science_matrix.sh --pilot`
2. `python3 scripts/aggregate_science_matrix.py --also-scan $RESULTS/KILOGAS066`
3. Long-chain top 2 IDs from scoreboard
4. Update this file with final production/validation IDs and numeric table
